import "resources/tests/LinkedList.obs"

main contract LinkedListAppendClient {
    // TODO: support arbitrary parameters to main.
    // For now, the parameters are limited to on-chain contract instances.
    transaction main(remote IntList list) {
        list.append(42);
    }
}