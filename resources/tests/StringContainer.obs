main contract StringContainer {
    string s;

    StringContainer() {
        s = "aaa";
    }

    transaction setS(string newS) returns string {
        string oldS = s;
        s = newS;
        return oldS;
    }
}