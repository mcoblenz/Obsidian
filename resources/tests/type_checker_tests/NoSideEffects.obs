// checks that there are errors for statements that don't have side effects

main contract NoSideEffects {

    transaction a() {
        int x = 2;
        x;
        1 + 3;
        1 < x;
        true or false;
    }

}