main contract Return {
        transaction f() returns int{
            return (4+4);
        }
        transaction g(){
	        int x = f();
            return;
        }

        transaction main() returns int{
            return f();
        }
}
