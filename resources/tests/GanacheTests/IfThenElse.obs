main contract IfThenElse {
    transaction main(int y) returns int {
        int x = 9;
        if (false) {
            x = 50;
        } else {
            x = 90 + y;
        }
        return x;
    }
}
