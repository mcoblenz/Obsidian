main contract CombinedExampleRet {
    transaction val() returns int {
        if ( true && ! false ) {
            return 4 + 20;
        } else {
            return 13;
        }
    }
    transaction main() returns int{
        int x;
        x = 9 + 0;
        x = val();
        return x;
    }
}
