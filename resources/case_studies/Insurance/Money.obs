// some sort of money
resource contract Money {
    int amount;

    Money@Owned(int amt) {
        amount = amt;
    }

    transaction addMoney(Money@Owned m) {
         amount = amount + m.amount;
         disown m;
    }

}
